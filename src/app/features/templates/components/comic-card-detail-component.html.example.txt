
//Reusable card with ng-template example.

<div class="container">
  <div class="row d-flex justify-content-center">
    <div class="col">
      <div class="card">
        <div *ngIf="comics; else digitalComicsTemplate">
          <a routerLink="/comics/comic-detail/{{ comics.id }}">
            <img
              class="card-img-top img-fluid d-block mx-auto"
              src="{{ comics.thumbnail.path }}.{{ comics.thumbnail.extension }}"
              alt="Comic Image Not found"
            />
          </a>
          <h5 class="card-title">{{ comics.title }}</h5>
          <br />
          <p class="author">
            {{ comics.creators.items[0].name }}
          </p>
        </div>
        <ng-template #digitalComicsTemplate>
          <div class="card-container-digital">
            <img
              class="card-img-top img-fluid d-block mx-auto"
              src="{{ digitalComics.thumbnail.path }}.{{
                digitalComics.thumbnail.extension
              }}"
              alt="Comic Image Not found"
            />
            <h5 class="card-title">{{ digitalComics.title }}</h5>
          </div>
        </ng-template>
      </div>
    </div>
  </div>
</div>
